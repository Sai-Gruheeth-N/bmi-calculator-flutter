import 'package:flutter/material.dart';

const kCardTextColor = Color(0xFF8D8E98);
const kLabelTextStyle = TextStyle(fontSize: 18.0, color: kCardTextColor);

const kBottomContainerHeight = 80.0;
const kActiveCardColor = Color(0xFF1D1E33);
const kInactiveCardColor = Color(0xFF111328);
const kBottomContainerColor = Color(0XFFEB1555);

//all constant start with k => user preference. This gives an ease in coding when constants are needed.
// Just type k and all constant pop up in the drop down suggestions.

const kNumberTextStyle = TextStyle(fontSize: 50, fontWeight: FontWeight.w900);

const kLargeButtonTextStyle =
    TextStyle(fontSize: 20.0, fontWeight: FontWeight.bold);

const kTitleTextStyle = TextStyle(fontSize: 30.0, fontWeight: FontWeight.bold);

const kResultTextStyle = TextStyle(
    color: Color(0xFF24E876), fontSize: 25.0, fontWeight: FontWeight.bold);

const kBMITextStyle = TextStyle(fontSize: 100.0, fontWeight: FontWeight.bold);
const kBodyTextStyle = TextStyle(fontSize: 22.0);
